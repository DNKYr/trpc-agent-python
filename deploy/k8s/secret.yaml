# 密钥（对应 SecretStore 的 K8s Secret 后端）

apiVersion: v1
kind: Secret
metadata:
  name: tenant-secrets
type: Opaque
stringData:
  # 生产用 SealedSecret/ExternalSecrets 注入，勿明文提交
  sql_url: "mysql+pymysql://root:CHANGE_ME@mysql:3306/trpc_agent"
  model_api_key: "CHANGE_ME"
  im_token: "CHANGE_ME"
  im_secret: "CHANGE_ME"
